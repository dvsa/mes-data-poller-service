name: CI

on:
  pull_request:
    branches:
      - develop
      - 'release-**'

jobs:
  runs-ci:
    uses: dvsa/des-workflow-actions/.github/workflows/ci-backend.yaml@sonar
    secrets:
      inherit
